'use strict';

<% if (typeof generateCrudRoutes !== 'undefined' && generateCrudRoutes && typeof documentCollections !== 'undefined') {
  documentCollections.forEach(function (collection) { -%>
module.context.use('/<%- collection %>', require('./api/<%- collection %>'), '<%- collection %>');
<% })
} -%>
<% if (typeof generateCrudRoutes !== 'undefined' && generateCrudRoutes && typeof edgeCollections !== 'undefined') {
  edgeCollections.forEach(function (collection) { -%>
module.context.use('/<%- collection %>', require('./api/<%- collection %>'), '<%- collection %>');
<% })
} -%>
